apiVersion: v1
kind: Service
metadata:
   name: profile-endpoint
   namespace: pizzanow
spec:
   selector:
      app: profile-service
   ports:
   - port: 4018
     targetPort: 2025
   type: ClusterIP